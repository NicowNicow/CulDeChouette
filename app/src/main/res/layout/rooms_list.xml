<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/roomElement"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/roomName"
        android:layout_width="271dp"
        android:layout_height="35dp"
        android:fontFamily="@font/faith"
        android:textColor="@color/yellow"
        android:textSize="30sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/roomCapacity"
        android:layout_width="87dp"
        android:layout_height="35dp"
        android:layout_marginStart="124dp"
        android:fontFamily="@font/faith"
        android:textColor="@color/yellow"
        android:textSize="30sp"
        app:layout_constraintBottom_toBottomOf="@+id/roomName"
        app:layout_constraintStart_toEndOf="@+id/roomName"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.0" />

    <ImageView
        android:id="@+id/lockIcon"
        android:layout_width="38dp"
        android:layout_height="35dp"
        android:adjustViewBounds="true"
        android:contentDescription="@string/imagePlaceholder"
        android:tint="@color/yellow"
        app:layout_constraintBottom_toBottomOf="@+id/roomCapacity"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.421"
        app:layout_constraintStart_toEndOf="@+id/roomCapacity"
        app:layout_constraintTop_toTopOf="@+id/roomCapacity"
        app:layout_constraintVertical_bias="0.0"
        android:src="@android:drawable/ic_lock_lock" />
</androidx.constraintlayout.widget.ConstraintLayout>